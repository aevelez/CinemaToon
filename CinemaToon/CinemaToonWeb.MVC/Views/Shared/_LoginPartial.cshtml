@using Microsoft.AspNetCore.Identity


@*@if (Context.User.Identity.IsAuthenticated)*@
@if (User.FindFirst(x => x.Type == "preferred_username") != null)
{
    <ul class="nav navbar-nav">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">@User.FindFirst(x => x.Type == "preferred_username").Value <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><a asp-action="Logout" asp-controller="Auth">Logout</a></li>
            </ul>
        </li>
    </ul>


}